version: '3.2'
services:
  minio:
    image: minio/minio:latest
    container_name: minio
    ports:
      - "9000:9000"
    restart: always
    command: server /data
    environment:
      MINIO_ACCESS_KEY: admin
      MINIO_SECRET_KEY: cuhk@VrdUy%mJ6c#Ts
    volumes:
      - ./minio/data:/data
