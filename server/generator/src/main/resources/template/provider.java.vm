package ${cfg.providerPackage};

import ${cfg.clientPackage}.${entity}Client;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

/**
 * ${table.comment}  Feign 提供者
 *
 * @author ${author}
 * @date ${date}
 */
#set($clientBasePath="#if(${cfg.clientPrefix})/${cfg.clientPrefix}#end#if(${package.ModuleName})/${package.ModuleName}#end/#if(${controllerMappingHyphenStyle})${controllerMappingHyphen}#else${table.entityPath}#end")
@RestController
@RequestMapping("${clientBasePath}")
public class ${entity}Provider implements ${entity}Client {

}